import React from "react";
import ReactDOM from "react-dom/client";
import AsesorDigital from "./AsesorDigital";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AsesorDigital />
  </React.StrictMode>
);
